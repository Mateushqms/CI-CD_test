name: Deploy .pbit ao Power BI

on:
  push:
    branches:
      - main
    paths:
      - "CI_CD_PBIT/dashboards/**/*.pbit"

  workflow_dispatch:
    inputs:
      dashboard:
        description: "Nome do arquivo .pbit (ex: vendas.pbit). Vazio = publica todos os alterados."
        required: false
        default: ""

jobs:
  deploy:
    name: Publicar .pbit no Power BI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependências
        run: pip install -r CI_CD_PBIT/scripts/requirements.txt

      - name: Determinar dashboards alterados
        id: changed
        run: |
          if [ -n "${{ github.event.inputs.dashboard }}" ]; then
            echo "files=CI_CD_PBIT/dashboards/${{ github.event.inputs.dashboard }}" >> "$GITHUB_OUTPUT"
          else
            FILES=$(git diff --name-only HEAD~1 HEAD -- 'CI_CD_PBIT/dashboards/*.pbit' | tr '\n' ' ')
            echo "files=$FILES" >> "$GITHUB_OUTPUT"
          fi

      - name: Publicar no Power BI
        env:
          PBI_TENANT_ID: ${{ secrets.PBI_TENANT_ID }}
          PBI_CLIENT_ID: ${{ secrets.PBI_CLIENT_ID }}
          PBI_CLIENT_SECRET: ${{ secrets.PBI_CLIENT_SECRET }}
          PBI_WORKSPACE_ID: ${{ secrets.PBI_WORKSPACE_ID }}
        run: |
          python CI_CD_PBIT/scripts/deploy_pbit_powerbi.py ${{ steps.changed.outputs.files }}
